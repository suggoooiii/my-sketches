{
  "name": "my-sketches",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "canvas-sketch --output=renders --stream=gif --fps=24 --open",
    "mp4": "canvas-sketch --output=renders --stream --open"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "canvas-sketch": "^0.7.7",
    "canvas-sketch-util": "^1.10.0",
    "three": "^0.147.0"
  }
}
